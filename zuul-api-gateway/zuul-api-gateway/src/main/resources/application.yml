server:
  port: 9000

spring:
  application:
    name: zuul-api-gateway
  main:
    web-application-type: reactive

eureka:
  client:
    enabled: true
    serviceUrl:
      defaultZone: http://localhost:8100/eureka